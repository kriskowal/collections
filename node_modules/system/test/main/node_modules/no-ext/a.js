var test = require("test");
test.assert(require.main !== module, "require.main !== module in dependency");
test.assert(require.main.id === "program.js", "main is program from within dependency");
module.exports = 30;
